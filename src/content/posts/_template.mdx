---
# 复制本文件，重命名为 `YYYY-MM-DD-英文短横线-标题.mdx`（文件名即链接）。
# 文件名里的日期要和下面的 publishedAt 一致；slug 只用小写字母、数字、连字符。
# 下划线开头的文件不会被发布，可放心保留本模板。
title: "一句话标题：这次解决了什么"
publishedAt: 2026-01-01
# updatedAt: 2026-01-15        # 可选：文章发布后又修订时填，会显示「更新于」
summary: "用一句人话讲清楚：这期我们解决了什么问题、用户能感受到什么变化。"  # 不超过 120 字
# 作者用「id」，不是显示名；id 必须在 src/content/authors.json 里登记过。
authors: ["lilink-team"]
# 标签也用「id」，最多 4 个，且都要在 src/content/tags.json 里登记过。
# 可选：product / engineering / design / privacy / community / growth / milestone
tags: ["product"]
# 发布状态：draft（草稿）→ review（待审）→ published（已发布）。
# 只有 published 会进入正式构建；draft/review 只在本地 `npm run dev` 可见。
status: draft
# slug: "custom-url"            # 可选：覆盖链接里的 slug（默认用文件名），一般不需要
# featured: true                # 偶发的里程碑更新可开启，在时间线上轻微强调
# cover: "./assets/封面.png"     # 可选封面，放在同目录的 assets/ 下
# coverAlt: "封面的替代文字"      # 一旦设置了 cover，就必须同时写 coverAlt
---

写给用户看，不写技术黑话。推荐三段式：

## 遇到了什么问题

描述一个真实的、用户能共鸣的小痛点。

## 我们做了什么

平实地讲改动思路与取舍，体现"认真对待每一份期待"。

## 现在的体验

改完之后用户会感受到什么。可以配 before / after 截图。
